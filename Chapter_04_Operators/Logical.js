ā/*Logical operators are used to combine multiple boolean expressions and return a boolean value based on the logical relationship between them.Here are some common logical operators in JavaScript:

1. && (Logical AND): Returns true if both operands are true, otherwise returns false.   
Example:let a = true;
let b = false;
console.log(a && b); // false
2. || (Logical OR): Returns true if at least one of the operands is true, otherwise returns false.
Example:        
let a = true;   
let b = false;
console.log(a || b); // true
3. ! (Logical NOT): Returns true if the operand is false, and returns false if the operand is true. 
Example:    
let a = true;   
console.log(!a); // false


*/
